#include <bits/stdc++.h>
using namespace std;

void solution() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    // Output x = b, y = c, z = c
    cout << b << " " << c << " " << c << endl;
}

int main() {
    int t;
    cin >> t;
    while (t--)
        solution();
    return 0;
}
